<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FuelLog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/dark_background</item>
    </style>

    <color name="colorPrimary">@color/dark_background</color>
    <color name="textColor">@color/light_background</color>
</resources>